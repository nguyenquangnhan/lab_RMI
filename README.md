# Banking-RMI
run app:
 open BankServer : run public static void main(String[] args)  it show windown chose modify run configuration touch it.
  Program arguments  write ( configuration.txt)
  orking directory: write ($ProjectFileDir$)
  apply and run.
open BankClient : run public static void main(String[] args)  it show windown chose modify run configuration touch it
    Program arguments write (localhost accounts or IP accounts)
    apply and run


